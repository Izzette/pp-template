# manifests/01_default.pp

node default {
  # Place puppet code for default nodes here.  Hopefully, this will not be needed and
  # default declarations can be included in hiera.
}

# vim: set ts=2 sw=2 et syn=puppet:
